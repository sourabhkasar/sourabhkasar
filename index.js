const age = 24;
const name = 'sourabh';

const dob = '30/01/2001'

const isadult = () => {
    if (age >= 18) {
        return true;
    }
    else
        return false;
    function isAdult() {
    mahes = [];
    
}